# PRD: 영어 명언 학습 앱

## 1. 개요
React Native(Expo) 기반의 개인용 영어 학습 미니 앱이다. 영어 명언과 저자를 보여주고, 발음을 듣고, 명언을 가린 뒤 외워서 타이핑하는 과정을 통해 암기 학습을 돕는다. 사용자는 개발자 본인 1인이며, 오늘 하루 실습 목적으로 React Native를 처음 배우며 만든다. 배포는 Expo Go(EAS Update)와 APK 직접 설치(EAS Build) 두 가지를 함께 지원한다.

> **참고(2026-08-03 업데이트)**: DeepL 번역 프록시 서버와 카카오톡 공유용 랜딩페이지는 모두 Vercel에 배포될 예정이나, Vercel 배포 작업 자체는 추후 별도로 진행하기로 했다. 이번 PRD 검토 범위는 **앱(React Native) 쪽 구현**에 한정하며, Vercel 배포와 직접 연관된 항목은 현재 검토 대상이 아니다(관련 내용은 9. 향후고려사항 참고).

## 2. 문제정의
영어 명언을 그냥 읽기만 하면 발음을 확인하기 어렵고 기억에 오래 남지 않는다. 직접 손으로 타이핑하며 능동적으로 회상(active recall)하는 방식의 암기 도구가 마땅치 않고, 로그인 없이 개인이 가볍게 쓸 수 있는 무료 명언 학습 앱도 찾기 어렵다. 이 프로젝트는 "읽기 → 듣기 → 가리고 쓰기 → 정답 확인"의 최소 학습 루프를 개인용으로 빠르게 구현하는 것을 목표로 한다.

## 3. 목표(Goals/Non-goals)

**Goals (이번 범위)**
- 영어 명언과 저자를 한 화면에서 확인할 수 있다.
- 듣기 버튼으로 명언의 영어 발음을 들을 수 있다.
- 명언을 가리고 타이핑한 뒤 정답 여부를 확인할 수 있다.
- Expo Go(EAS Update) / APK 직접 설치(EAS Build) 두 가지 방식으로 배포한다.

**Non-goals(이번 범위 아님)**
- 로그인/회원가입, 다중 사용자 지원
- OAuth 기반 카카오톡 자동 발송
- 학습 기록 저장, 학습 알림(푸시)
- 즐겨찾기, 카테고리 필터 등 부가 기능(→ 9. 향후 고려사항)
- **Vercel 배포 일체(DeepL 번역 프록시 서버, 카카오톡 공유 랜딩페이지)** — 추후 별도로 진행하기로 결정, 이번 검토 대상 아님. 앱에서 한국어 번역이 필요한 화면은 당분간 목업/보류 상태로 둔다.

## 4. 핵심기능

| 기능 | 설명 | 우선순위 |
|---|---|---|
| 명언 표시 | DummyJSON Quotes API(키 불필요)에서 영어 명언과 저자를 가져와 표시. **100자 이하 명언만 노출(기본값, 추후 조정 가능)** | P0 |
| 듣기 버튼 | 기기 내장 음성합성(TTS)으로 영어 명언 발음 재생 | P0 |
| 암기 테스트 | 명언 텍스트를 가리고 사용자가 타이핑 → 제출 시 원문과 비교하여 정답/오답 판정. **재시도 없이 결과만 표시(기본값, 추후 조정 가능)** | P0 |
| 다음 명언 넘기기 | 버튼으로 새 명언을 불러와 학습을 이어감(한 번에 1개씩, **완전 랜덤 표시, 기본값**) | P0 |
| EAS Update + Expo Go 배포 | Android·iOS 공통, Expo Go 앱 설치 후 QR/링크로 실행 | P1 |
| EAS Build APK 배포 | Android 전용, Expo Go 없이 APK 직접 설치·실행 | P1 |
| 예외 처리 | 명언 API 실패, 네트워크 오류 시 사용자 안내 및 화면 유지 | P2 |
| ~~한국어 번역 표시~~ | Vercel 서버리스 함수가 DeepL API를 프록시 호출하여 번역 결과를 앱에 전달·표시 | 보류(Vercel 배포 시 진행, 이번 검토 대상 아님) |
| ~~랜딩페이지 + 카카오톡 공유~~ | Vercel에 별도 정적 웹페이지 배포, 로그인 없는 Kakao JS SDK로 공유하기 버튼 제공 | 보류(Vercel 배포 시 진행, 이번 검토 대상 아님) |

## 5. 사용자 시나리오

**시나리오 1 — 기본 학습 루프**
1. 앱 실행 시 영어 명언 1개와 저자가 표시된다. (한국어 번역은 Vercel 배포 전까지 미표시/목업으로 둔다)
2. [듣기] 버튼을 눌러 영어 발음을 듣는다.
3. [암기하기] 버튼을 누르면 명언 텍스트가 가려지고 입력창이 나타난다.
4. 기억나는 대로 타이핑 후 [정답 확인]을 누른다.
5. 대소문자·공백 차이만 관대하게 허용한 뒤 원문과 비교해 정답/오답을 표시한다. 재시도 없이 결과만 보여준다(기본값).
6. [다음 명언] 버튼으로 완전 랜덤한 새 명언으로 넘어간다(기본값).

**시나리오 2 — 앱 설치(신규 사용자 관점, 실제로는 본인)**
1. Expo Go로 실행하는 방법(QR/링크) 또는 APK 다운로드 중 원하는 방식을 선택한다.
2. 안내에 따라 앱을 실행한다.

**시나리오 3 — 예외 상황**
1. 명언 API 호출이 실패하거나 네트워크가 끊긴 경우, 안내 문구를 표시한다.
2. 재시도 또는 이전 명언 유지 등으로 사용자가 학습을 이어갈 수 있게 한다.

> 랜딩페이지 접속 및 카카오톡 공유 시나리오는 Vercel 배포가 진행된 이후 별도로 정의한다(9. 향후고려사항 참고).

## 6. 기술요구사항
- **프로젝트 생성**: Expo SDK 54 고정.
- **명언 데이터**: DummyJSON Quotes API(`https://dummyjson.com/quotes/random`), 별도 키 불필요. 매번 완전 랜덤 호출(기본값), 100자를 초과하는 명언은 걸러내고 재요청(기본값, 추후 조정 가능).
- **다크모드**: 지원하지 않음, 라이트 모드로 고정(기본값, 추후 조정 가능).
- **발음(TTS)**: `expo-speech` 등 Expo 내장 음성합성 모듈 사용, 별도 키 불필요.
- **배포 방식 1**: `eas update`로 Expo Go 환경에 배포(Android·iOS 공통, Expo Go 설치 필요).
- **배포 방식 2**: `eas build --platform android`로 APK 빌드 후 직접 배포(Android 전용, Expo Go 불필요).
- **개발 순서(이번 범위, 수정됨)**: ① 화면을 하드코딩 데이터로 완성 → ② 예외 처리 → ③ EAS Update/Build 배포 확인.
- **상태관리**: 별도 라이브러리 없이 `useState`/`useEffect` 수준으로 충분(개인용 소규모 앱).

**보류(Vercel 배포 이후 진행, 이번 검토·개발 대상 아님)**
- **번역**: DeepL API Free 플랜. 앱은 Vercel 서버리스 함수(API Route)만 호출하고, 이 함수 내부에서만 DeepL 키를 환경변수로 보관해 DeepL을 호출한다. 클라이언트 번들에는 키가 절대 포함되지 않는다.
- **랜딩페이지**: 앱과 별도로 Vercel에 정적 웹페이지로 배포. Kakao JS SDK는 브라우저 환경에서 동작하므로 RN 앱이 아닌 이 웹페이지에 탑재한다. 카카오 디벨로퍼스에서 발급한 JavaScript 키와 도메인 등록이 필요하다.
- Vercel 배포가 시작되면 개발 순서에 "번역 프록시 서버 준비 → 실제 API로 교체" 단계를 다시 추가한다.
- **보안**: DeepL 키는 Vercel 프로젝트 환경변수로만 저장하고, 서버리스 함수 응답에는 번역 결과만 포함한다(추후 적용).

## 7. UI 구성

**메인(학습) 화면**
- 영어 명언 텍스트
- 저자명
- (번역 영역은 Vercel 배포 전까지 비표시 또는 "번역 준비 중" placeholder)
- 버튼: [듣기] · [암기하기] · [다음 명언]
- [암기하기] 클릭 시: 명언 텍스트를 가리고 텍스트 입력창 + [정답 확인] 버튼 노출
- 정답 확인 후: 정답/오답 여부 표시, 원문 다시 노출

**랜딩 웹페이지(Vercel)** — 보류, 이번 검토 대상 아님
- 앱 소개 문구
- Expo Go 실행 안내(QR 코드 또는 링크)
- [APK 다운로드] 버튼
- [카카오톡 공유하기] 버튼

## 8. 성공지표
개인 실습 프로젝트이므로 정량 지표보다 완료 기준(Definition of Done)으로 판단한다.
- 명언·저자·번역이 화면에 정상적으로 표시된다.
- [듣기] 버튼으로 영어 발음이 재생된다.
- 암기 테스트에서 정답/오답이 정확히 판정된다(대소문자·공백만 관대하게 처리).
- Expo Go로 QR/링크를 통해 앱이 실행된다.
- EAS Build APK를 설치해 Expo Go 없이 앱이 실행된다.

> 아래 항목은 Vercel 배포가 진행된 이후에 적용되는 완료 기준이며, 이번 검토 대상이 아니다.
> - 랜딩페이지의 카카오톡 공유 버튼이 정상 동작한다.
> - 개발자 도구/네트워크 탭 확인 결과 DeepL API 키가 클라이언트에 노출되지 않는다.

## 9. 향후 고려사항
- **Vercel 배포 진행** — DeepL 번역 프록시 서버 구축 및 배포, 카카오톡 공유 랜딩페이지 제작 및 배포(Kakao JS SDK 연동). 이번 실습에서는 의도적으로 제외했으며, 진행 시 6. 기술요구사항의 "보류" 항목과 4. 핵심기능 표의 보류 항목을 다시 활성화한다.
- 로그인 및 사용자별 학습 기록 저장
- 즐겨찾기, 카테고리별 명언 필터링
- 학습 알림(푸시 알림)
- 오답 노트 및 간격 반복(spaced repetition) 복습
- OAuth 기반 카카오톡 자동 공유/로그인
- iOS 배포 방식 확대(TestFlight 등)
- 음성 인식 기반 발음 정확도 평가

---

## 부록: Open Questions
아래 항목은 모두 확정되어 기본값으로 진행하기로 했다(2026-08-03). 실제 사용해보면서 필요하면 언제든 조정 가능하다.
- ~~DummyJSON 명언을 전체 랜덤으로 쓸지, 특정 카테고리/개수로 제한할지~~ → **확인 완료**: `/quotes/random`, `/quotes` 응답 스키마가 `{id, quote, author}`뿐이라 category/tag 필드 자체가 없음. 카테고리 필터링은 불가능하며 전체 랜덤만 가능.
- ~~[다음 명언]이 완전 랜덤인지 순차 진행인지~~ → **기본값: 완전 랜덤**(`/quotes/random` 매 호출).
- ~~오답 시 재시도를 허용할지~~ → **기본값: 재시도 없음**, 정답/오답 결과만 표시하고 [다음 명언]으로 이동.
- ~~다크모드 지원 여부~~ → **기본값: 미지원**, 라이트 모드 고정.
- ~~명언 글자 수 상한을 둘지 여부~~ → **기본값: 100자 이하 명언만 노출**, 초과 시 재요청.

**Vercel 배포 진행 시점에 다시 확인할 항목**(현재는 보류)
- 카카오 디벨로퍼스 앱 등록/JavaScript 키 발급 및 도메인 등록을 사용자가 준비했는지 여부
- 랜딩페이지 도메인(Vercel 기본 도메인 vs 커스텀 도메인)
