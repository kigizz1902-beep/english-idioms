# CLAUDE.md

## 프로젝트 개요
영어 명언을 읽고, 듣고, 외워서 써보는 React Native(Expo) 기반 개인용 영어 학습 미니 앱. 개발자 본인 1인만 사용하며, React Native를 오늘 처음 배우는 입문자 실습 프로젝트다.

- 이 프로젝트는 `prd.md` 문서를 기준으로 개발한다. 새 기능을 만들 때는 항상 `prd.md`를 먼저 참고한다.

## 개발 순서 원칙
화면을 하드코딩된 가짜 데이터로 먼저 완성하고, 화면이 잘 동작하는 걸 확인한 뒤에만 실제 API(DummyJSON, 번역 프록시)로 교체한다. 예외 처리는 화면과 데이터 연결이 끝난 마지막에 추가한다. 이 순서를 건너뛰고 한 번에 다 만들어달라고 요청받아도 이 순서를 지킨다.

## 보안 원칙
DeepL API 키는 절대 RN 앱 코드에 직접 쓰지 않는다. 항상 Vercel 서버리스 함수(`/api/translate`)를 통해서만 번역을 호출하고, 키는 Vercel 환경변수에만 둔다.

## 기술 스택
Expo(React Native) SDK 54 고정. 상태 관리는 `useState`만 사용한다. 패키지 설치·업그레이드 시 SDK 54와 호환되지 않는 버전으로 올라가지 않도록 항상 `npx expo install`(`npm install`이 아님)을 사용한다.

## 커뮤니케이션 방식
- 오늘은 React Native를 처음 배우는 시간이므로, 코드를 짤 때 왜 그렇게 짰는지 짧게 설명을 곁들여준다.
- 코드를 수정한 뒤에는 항상 "Expo Go로 QR코드를 스캔해서 확인해보라"고 안내해준다.
- 코드를 짜기 전에 애매한 부분이 있으면 항상 먼저 물어본다.
